version: "3.9"

services:

  gateway:

    build: .

    container_name: x402-gateway-service

    restart: unless-stopped

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - .:/app
